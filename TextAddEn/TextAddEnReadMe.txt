★ここでは以下を扱っています。
★Read Me.

・文字列の検索と削除(英語。もちろん日本語も可能)
Search and deletion of a character string. 
・文字列を検索して特定の文字列があった場合、特定の範囲内でランダムな位置に画像を表示
When a character string is searched and there is the character string "app" or "store", 
a picture is displayed on a random position in the specific range. 

1.Single View Applicationで新規プロジェクトを作る
New project is made(Single View Application). 

2.UIパーツを設置、ViewController.hと結びつける
UIpart is connected to ViewController.h. 

3.ViewController.hのボタンとText FieldのActionに追加と書かれている部分を書く
ViewController.h and .m add code.